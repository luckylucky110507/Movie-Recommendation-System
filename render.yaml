services:
  - type: web
    name: movie-recommendation-system
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run "Recommendation system.py" --server.port $PORT --server.address 0.0.0.0
